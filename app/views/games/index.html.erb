<div class="row">
  <% @games.each do |game| %>
    <div class="col-md-4">
        <div class="booyah-box col-xs-10 col-xs-offset-1">
          <h1><%= game.title %></h1>
          <h2><%= game.description %></h2>
        </div>
      </div>
  <% end %>
</div>
